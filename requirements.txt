# --- Core ML ---
torch>=2.2
torchvision>=0.17
numpy>=1.26,<3
scipy>=1.11
scikit-learn>=1.4
scikit-image>=0.22
opencv-python-headless>=4.9
Pillow>=10.2

# --- Model export / serving ---
onnx>=1.15
onnxruntime>=1.17
onnxscript>=0.1  # required by torch.onnx.export's dynamo path

# --- API ---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
python-multipart>=0.0.9
SQLAlchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1
boto3>=1.34

# --- Security ---
# bcrypt directly rather than passlib: passlib is unmaintained and its bcrypt
# backend breaks against bcrypt >= 4.1 (see api/security/auth.py).
bcrypt>=4.1
pyjwt>=2.8
cryptography>=42.0

# --- Reporting / CLI ---
matplotlib>=3.8
tqdm>=4.66
typer>=0.9
tabulate>=0.9

# --- Dev / test ---
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
ruff>=0.3
